1.每一个程序在执行的时候都占用一块可用的内存空间，专门用于存放动态分配的对象，此内存空间称为程序的自由存储区或堆。
  C语言中用malloc和free表示，c++用new 和delete
2.int *a=new int[10];创建的是匿名的，只能通过间接访问地址来访问值（在自由存储区中创建的数组对象是没有名字的）
3.int *a=new int[10]（），这样，虽然他是内置类型，但是还是可以初始化。
