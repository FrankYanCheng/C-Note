1.头文件一般用于声明，不用于定义（除了constant 和inline)
因为可以声明多次，但只能定义一次。所以如果在头文件中定义了的话，在引用该头文件的不同源文件中，就相当于是定义了多次。
但是const除外，规则是定义了只能在该文件下使用。默认是局部变量。所以可以在头文件中定义，就符合了上述的规则。源文件中的const都指向一个地址。
如果需要其他的方法来实现
不定义consts的时候，在头文件中声明，源文件中定义，就可以被共享。但是这种情况下的定义不应该是局部的吗？
2.好处是当声明修改时，只需要修改头文件即可。
3.